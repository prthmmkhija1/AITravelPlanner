{
  "name": "ai-travel-planner",
  "private": true,
  "description": "AI Travel Planner - React + FastAPI",
  "scripts": {
    "setup": "npm --prefix frontend install && .\\venv\\Scripts\\pip.exe install -r requirements.txt",
    "frontend": "npm --prefix frontend run dev",
    "server": "cd backend && ..\\venv\\Scripts\\uvicorn.exe api_server:app --reload --host 0.0.0.0 --port 8000",
    "dev": "echo Run 'npm run frontend' and 'npm run server' in separate terminals",
    "build": "npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=18"
  }
}
